<%= form_for post do |f|%>
<%= f.label "Title" %>
<%= f.text_field :title %>
<%= f.label "Content" %>
<%= f.text_area :content %>
<%= f.label "Categories" %>
<%= f.collection_select :categories, Category.all, :id, :name %>
<%= f.label "Categories" %>
<%= f.collection_select :categories, Category.all, :id, :name %>
<%end%>
